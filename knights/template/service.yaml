apiVersion: v1
kind: Service
metadata:
  name: knight-TEMPLATE
  namespace: roundtable
spec:
  selector:
    app.kubernetes.io/name: knight-TEMPLATE
  ports:
    - name: gateway
      port: 18789
      targetPort: gateway
    - name: health
      port: 8080
      targetPort: health
