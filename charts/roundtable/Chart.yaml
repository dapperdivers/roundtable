apiVersion: v2
name: roundtable
description: "Knights of the Round Table — Multi-agent AI platform for Kubernetes"
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: nats
    version: "1.2.*"
    repository: https://nats-io.github.io/k8s/helm/charts/
    condition: nats.enabled
  - name: redis
    version: "19.*"
    repository: https://charts.bitnami.com/bitnami
    condition: redis.enabled
