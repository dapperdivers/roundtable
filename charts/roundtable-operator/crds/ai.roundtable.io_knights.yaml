---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: knights.ai.roundtable.io
spec:
  group: ai.roundtable.io
  names:
    kind: Knight
    listKind: KnightList
    plural: knights
    singular: knight
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.domain
      name: Domain
      type: string
    - jsonPath: .spec.model
      name: Model
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .status.tasksCompleted
      name: Tasks
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Knight is the Schema for the knights API.
          A Knight represents a specialized AI agent in the Round Table,
          deployed as a Kubernetes workload with NATS JetStream connectivity,
          skill injection, and shared vault access.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Knight
            properties:
              arsenal:
                description: arsenal configures the skill arsenal git-sync sidecar.
                properties:
                  image:
                    default: registry.k8s.io/git-sync/git-sync:v4.4.0
                    description: image overrides the git-sync container image.
                    type: string
                  period:
                    default: 300s
                    description: period is how often to sync (e.g., "300s").
                    type: string
                  ref:
                    default: main
                    description: ref is the git ref to sync.
                    type: string
                  repo:
                    default: https://github.com/dapperdivers/roundtable-arsenal
                    description: repo is the git repository URL containing skills.
                    type: string
                type: object
              concurrency:
                default: 2
                description: concurrency is the maximum number of concurrent tasks
                  the knight can process.
                format: int32
                maximum: 10
                minimum: 1
                type: integer
              domain:
                description: |-
                  domain is the knight's area of expertise (e.g., "security", "infrastructure", "finance").
                  Used for NATS subject routing and skill filtering.
                minLength: 1
                type: string
              env:
                description: env defines additional environment variables for the
                  knight container.
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
              envFrom:
                description: envFrom defines sources of environment variables (secrets,
                  configmaps).
                items:
                  description: EnvFromSource represents the source of a set of ConfigMaps
                    or Secrets
                  properties:
                    configMapRef:
                      description: The ConfigMap to select from
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                        optional:
                          description: Specify whether the ConfigMap must be defined
                          type: boolean
                      type: object
                      x-kubernetes-map-type: atomic
                    prefix:
                      description: |-
                        Optional text to prepend to the name of each environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    secretRef:
                      description: The Secret to select from
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                        optional:
                          description: Specify whether the Secret must be defined
                          type: boolean
                      type: object
                      x-kubernetes-map-type: atomic
                  type: object
                type: array
              image:
                default: ghcr.io/dapperdivers/pi-knight:latest
                description: image is the container image for the knight runtime.
                type: string
              model:
                default: claude-sonnet-4-20250514
                description: model is the AI model to use (e.g., "claude-sonnet-4-20250514",
                  "claude-haiku-35-20241022").
                type: string
              nats:
                description: nats configures the knight's NATS JetStream consumer
                  and subjects.
                properties:
                  consumerName:
                    description: |-
                      consumerName overrides the auto-generated durable consumer name.
                      Defaults to "knight-{name}".
                    type: string
                  maxDeliver:
                    default: 1
                    description: maxDeliver is the maximum number of delivery attempts
                      per message.
                    format: int32
                    minimum: 1
                    type: integer
                  resultsStream:
                    default: fleet_a_results
                    description: resultsStream is the JetStream stream to publish
                      results to.
                    type: string
                  stream:
                    default: fleet_a_tasks
                    description: stream is the JetStream stream name to consume from.
                    type: string
                  subjects:
                    description: |-
                      subjects defines the JetStream filter subjects for task consumption.
                      e.g., ["fleet-a.tasks.security.>"]
                    items:
                      type: string
                    minItems: 1
                    type: array
                  url:
                    default: nats://nats.database.svc:4222
                    description: url is the NATS server URL.
                    type: string
                required:
                - subjects
                type: object
              prompt:
                description: prompt allows overriding the knight's system prompt components.
                properties:
                  configMapRef:
                    description: |-
                      configMapRef references a ConfigMap containing prompt overrides.
                      Keys: "AGENTS.md", "TOOLS.md", "SOUL.md"
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  identity:
                    description: identity overrides the knight's identity/persona
                      description.
                    type: string
                  instructions:
                    description: instructions provides additional instructions appended
                      to the system prompt.
                    type: string
                type: object
              resources:
                description: resources defines compute resource requirements for the
                  knight container.
                properties:
                  cpu:
                    anyOf:
                    - type: integer
                    - type: string
                    default: 200m
                    description: cpu is the CPU limit for the knight container.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  memory:
                    anyOf:
                    - type: integer
                    - type: string
                    default: 256Mi
                    description: memory is the memory limit for the knight container.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                type: object
              skills:
                description: |-
                  skills defines which skill categories this knight has access to.
                  The operator will configure the skill-filter sidecar accordingly.
                items:
                  type: string
                minItems: 1
                type: array
              suspended:
                default: false
                description: suspended, if true, scales the knight deployment to 0
                  replicas.
                type: boolean
              taskTimeout:
                default: 120
                description: taskTimeout is the default task timeout in seconds.
                format: int32
                maximum: 600
                minimum: 30
                type: integer
              tools:
                description: tools defines additional system packages and tools the
                  knight needs.
                properties:
                  apt:
                    description: apt is a list of apt packages to install (fallback,
                      requires root — prefer nix).
                    items:
                      type: string
                    type: array
                  mise:
                    description: mise is a list of tools to install via mise (e.g.,
                      "shodan", "kubectl").
                    items:
                      type: string
                    type: array
                  nix:
                    description: |-
                      nix is a list of nixpkgs packages to install via Nix flakes (e.g., "nmap", "whois", "dnsutils").
                      These get compiled into a flake.nix and built on first boot, cached on the Nix PVC.
                    items:
                      type: string
                    type: array
                type: object
              vault:
                description: vault configures the shared Obsidian vault mount.
                properties:
                  claimName:
                    default: obsidian-vault
                    description: claimName is the PVC name for the shared vault.
                    type: string
                  readOnly:
                    default: true
                    description: readOnly mounts the base vault as read-only.
                    type: boolean
                  writablePaths:
                    default:
                    - Briefings/
                    - Roundtable/
                    description: writablePaths are subpaths within the vault the knight
                      can write to.
                    items:
                      type: string
                    type: array
                type: object
              workspace:
                description: workspace configures the knight's persistent workspace.
                properties:
                  existingClaim:
                    description: |-
                      existingClaim references an existing PVC to use instead of creating a new one.
                      Useful for migrating existing knights to operator management.
                    type: string
                  size:
                    default: 1Gi
                    description: size is the storage request for auto-created PVCs.
                    type: string
                type: object
            required:
            - domain
            - nats
            - skills
            type: object
          status:
            description: status defines the observed state of Knight
            properties:
              conditions:
                description: conditions represent the current state of the Knight
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastTaskAt:
                description: lastTaskAt is the timestamp of the last completed task.
                format: date-time
                type: string
              natsConsumer:
                description: natsConsumer is the name of the reconciled NATS durable
                  consumer.
                type: string
              observedGeneration:
                description: observedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: phase is the current lifecycle phase of the knight.
                enum:
                - Pending
                - Provisioning
                - Ready
                - Degraded
                - Suspended
                type: string
              ready:
                description: ready indicates whether the knight is ready to accept
                  tasks.
                type: boolean
              tasksCompleted:
                description: tasksCompleted is the total number of tasks completed
                  since creation.
                format: int64
                type: integer
              tasksFailed:
                description: tasksFailed is the total number of tasks that failed.
                format: int64
                type: integer
              totalCost:
                description: totalCost is the cumulative cost in USD of all tasks
                  processed.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
